#!/usr/bin/env python
# -*- coding: utf-8 -*-
# generated by wxGlade 0.6.5 on Sat Oct 19 15:57:19 2013

import wx
import pyex
import os
# begin wxGlade: extracode
# end wxGlade


class MyFrame1(wx.Frame):
    def __init__(self, *args, **kwds):
        # begin wxGlade: MyFrame1.__init__
        kwds["style"] = wx.DEFAULT_FRAME_STYLE
        wx.Frame.__init__(self, *args, **kwds)
        self.Tester_copy = wx.Button(self, -1, "Test database")
        self.Close_copy = wx.Button(self, -1, "Close App")
        self.button_2 = wx.Button(self, -1, "Choose Directory")
        self.label_1 = wx.StaticText(self, -1, "output:\n")

        self.__set_properties()
        self.__do_layout()

        self.Bind(wx.EVT_BUTTON, self.tester, self.Tester_copy)
        self.Bind(wx.EVT_BUTTON, self.closer, self.Close_copy)
        self.Bind(wx.EVT_BUTTON, self.on_dir, self.button_2)
        # end wxGlade

        self.path_choice = ""  # Path for directory searching
        # test types
        self.testtypes = ["MP3Metadata Test: ", "MP3 Data Storage Test: ",
                          "Directory Test: "]

    def __set_properties(self):
        # begin wxGlade: MyFrame1.__set_properties
        self.SetTitle("hipbit tester")
        _icon = wx.EmptyIcon()
        _icon.CopyFromBitmap(wx.Bitmap("folder_music.ico", wx.BITMAP_TYPE_ANY))
        self.SetIcon(_icon)
        self.SetSize((400, 95))
        self.label_1.SetMinSize((260, 95))
        # end wxGlade

    def __do_layout(self):
        # begin wxGlade: MyFrame1.__do_layout
        sizer_4 = wx.BoxSizer(wx.HORIZONTAL)
        sizer_5 = wx.BoxSizer(wx.VERTICAL)
        sizer_5.Add(self.Tester_copy, 0, 0, 0)
        sizer_5.Add(self.Close_copy, 0, 0, 0)
        sizer_5.Add(self.button_2, 0, 0, 0)
        sizer_4.Add(sizer_5, 1, wx.SHAPED, 0)
        sizer_4.Add(self.label_1, 0, wx.LEFT, 0)
        self.SetSizer(sizer_4)
        self.Layout()
        self.Centre()
        # end wxGlade

    def tester(self, event):  # wxGlade: MyFrame1.<event_handler>
        if self.path_choice == "":  # nothing in path
            self.label_1.SetLabel("Error, no path given.")
        else:
            #self.text = self.testtypes[0] + pyex.ID3test() + " \n" + self.testtypes[1] + pyex.AddDBtest() \
            #            + " \n" + self.testtypes[2] + pyex.mp3search(self.path_choice)
            pyex.realtest(self.path_choice)
            self.label_1.SetLabel("Done")
        event.Skip()

    def closer(self, event):  # wxGlade: MyFrame1.<event_handler>
        self.Close()
        event.Skip()

    def on_dir(self, event):  # wxGlade: MyFrame1.<event_handler>
        dlg = wx.DirDialog(self, "Choose a directory:",
                           style=wx.DD_DEFAULT_STYLE
                           #| wx.DD_DIR_MUST_EXIST
                           #| wx.DD_CHANGE_DIR
                           )
        if dlg.ShowModal() == wx.ID_OK:
            self.label_1.SetLabel(dlg.GetPath())
            self.path_choice = dlg.GetPath()  # set path for directory transversal
        dlg.Destroy()
        event.Skip()

# end of class MyFrame


class MyApp(wx.App):
    def OnInit(self):
        wx.InitAllImageHandlers()
        frame_1 = MyFrame1(None, -1, "")
        self.SetTopWindow(frame_1)
        frame_1.Show()
        return 1

# end of class MyApp

if __name__ == "__main__":
    app = MyApp()
    app.MainLoop()
